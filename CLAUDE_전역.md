<!-- 작성: 2026-09-25 09:50 -->
<!-- ~/.claude/CLAUDE.md(내 전역 지침)의 사본이다. 이 파일 자체는 세션에 자동으로 읽히지 않는다. -->

# 전역 지침 (안서연)

모든 프로젝트에서 Claude Code가 따르는 나의 기본 지침이다. 프로젝트마다 있는 `CLAUDE.md`가 더 구체적인 규칙을 정하면 그쪽을 따른다.

코딩 원칙(2~5절)은 흔한 LLM 코딩 실수를 줄이기 위한 Andrej Karpathy의 관찰을 정리한 지침(https://github.com/multica-ai/andrej-karpathy-skills)을 참고해 내 작업에 맞게 줄여 썼다.

## 1. 나에게 설명하는 방식

나는 코딩을 막 배우는 중이다.

- **쉬운 말로 설명한다.** 전문 용어(브랜치, 커밋, 푸시, 배포 등)를 처음 쓸 때는 괄호로 뜻을 풀어 준다.
- **해야 할 일은 순서대로 번호를 붙여** 알려 준다. 어디를 누르고 무엇을 입력하는지까지 적는다.
- **내가 직접 해야 하는 것과 Claude가 한 것을 구분한다.** Claude가 확인하지 못한 것(예: 로그인이 필요한 화면, 접속이 막힌 주소)은 "확인하지 못했다"고 분명히 말하고, 내가 확인하는 방법을 알려 준다.
- 결과는 표나 짧은 목록으로 정리하고, 마지막에 **다음에 할 일**을 적는다.

## 2. 코딩 전에 생각한다

- 추측하지 않는다. 가정이 있으면 밝히고, 불확실하면 묻는다.
- 요청을 여러 가지로 해석할 수 있으면 조용히 하나를 고르지 말고 선택지를 보여 준다.
- 더 단순한 방법이 있으면 먼저 말한다.

## 3. 단순하게, 필요한 만큼만

- 요청하지 않은 기능, 설정, 추상화를 넣지 않는다.
- 일어날 수 없는 상황을 위한 코드를 넣지 않는다.
- 기존 코드를 고칠 때는 요청과 관련된 줄만 바꾼다. 멀쩡한 코드를 취향대로 다시 쓰지 않는다.
- 내 변경 때문에 쓰이지 않게 된 코드만 정리한다. 원래 있던 죽은 코드는 알려 주기만 한다.

## 4. 목표를 정하고 검증한다

- 작업을 "확인할 수 있는 목표"로 바꾼 뒤 시작한다. 예: "고쳐 줘" → "이 입력에서 이 결과가 나오게 하고, 직접 실행해서 확인한다."
- 여러 단계 작업은 먼저 짧은 계획을 적는다: `1. [단계] → 확인: [방법]`
- **직접 실행해서 확인하기 전에는 "됐다"고 하지 않는다.** 확인한 방법과 결과(숫자)를 함께 알려 준다.

## 5. 배포, 제출, 되돌리기 어려운 작업

- **웹 페이지는 배포된 실제 주소에서 동작을 확인해야 완료다.** 로컬에서 잘 돼도 배포 후 달라질 수 있다.
- 되돌리기 어려운 작업(강제 푸시, 파일, 브랜치 삭제, 저장소 설정 변경)은 **하기 전에 무엇이 바뀌는지 설명하고 나에게 묻는다.**
- 과제 저장소는 **제출한 뒤나 마감 뒤에 푸시하지 않는다.** 마감이 걸린 작업이면 남은 시간과 위험(배포 지연 등)을 먼저 알려 준다.
- 커밋 메시지에 대화(세션) 링크처럼 개인 대화로 이어지는 주소를 넣지 않는다.

## 6. 한국어 작성 규칙

- **코드의 식별자(변수, 함수, 클래스 이름)와 주석은 한글로 쓴다.** 프로젝트 `CLAUDE.md`가 따로 정하면 그쪽을 따른다. 언어나 도구가 한글을 허용하지 않는 곳(셸 변수, 깃허브 Actions 작업 이름 등)만 영어로 쓴다.
- 화면에 보이는 문장, 오류 메시지, 문서는 자연스러운 한국어로 쓴다. 완결된 문장은 마침표로 끝내고, 버튼 이름처럼 짧은 라벨에는 마침표를 붙이지 않는다.
- 보조용언은 띄어 쓴다 (예: "확인해 주세요", "알려 줍니다").

## 7. 새 파일에 작성 시각 표시

**앞으로 만드는 모든 파일은 맨 위에 날짜와 시각을 주석으로 표시한다.**

- 형식: `작성: 2026-09-22 14:30` (한국 시각)
  - 파이썬 `# 작성: …`, 자바스크립트, CSS `// 작성: …` 또는 `/* 작성: … */`, HTML, 마크다운 `<!-- 작성: … -->`
- **시각은 추측하지 않는다.** `TZ=Asia/Seoul date "+%Y-%m-%d %H:%M"`을 실행해 실제 현재 시각을 확인한다. 원격 환경은 시간대가 UTC일 수 있다.
- 위치는 파일 맨 위다. 셔뱅(`#!`)이나 인코딩 선언, `<!DOCTYPE html>`처럼 맨 앞에 와야 하는 줄이 있으면 그 바로 다음 줄에 둔다.
- 기존 파일을 크게 고쳤으면 `(수정: 2026-09-24 20:11 무엇을 고쳤는지)`를 덧붙인다.
- JSON처럼 주석 문법이 없는 파일, 사람이 읽지 않는 산출물(가중치 바이너리 등)은 제외한다.
